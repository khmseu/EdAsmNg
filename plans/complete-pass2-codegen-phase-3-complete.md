## Phase 3 Complete: StorGMC and Object Memory Management

Implemented machine code storage to object memory/buffer by enabling StorGMC() which writes generated bytes from GMC[] buffer to memory.

**Files created/changed:**

- [src/lib/asm/asm.cpp](../src/lib/asm/asm.cpp)
- [tests/app_test.cpp](../tests/app_test.cpp)

**Functions created/changed:**

- StorGMC() - enabled and moved to compiled EdAsmNg::Asm namespace
- WriteObjMemory() - test helper for Phase 3
- SetLength(), GetLength(), SetLenTIdx(), GetLenTIdx() - test helpers
- SetGMC(), GetGMC() - GMC buffer accessors

**Tests created/changed:**

- SingleByteStorage - store one byte to ObjPC ✅
- MultiByteStorage - store 3 bytes (LDA abs) ✅
- ObjPC_Advances - verify ObjPC advances correctly ✅
- GenF_Suppression - test GenF N bit suppression ✅
- LengthZero_NoAction - test Length==0 case ✅
- DiskMode_Stubbed - disk mode stubbed for Phase 9+ ✅

**Test Results:** 6/6 passing ✅

**Review Status:** APPROVED

**Git Commit Message:**

```
feat: Phase 3 - Enable StorGMC for object code storage

- Enable StorGMC() from disabled block
- Implement memory-mode object code emission to g_test_obj_memory
- Stub disk-mode for Phase 9+ (Wr1Byte deferred)
- Add WriteObjMemory() test helper
- Add missing test helpers: SetLength, SetLenTIdx, SetGMC
- All 6 Phase 3 tests passing
```
